/**
 * @flowforge/workflow-engine — parser, graph mapper, export builder (stub).
 * Will map internal workflow to React Flow nodes/edges and n8n JSON.
 */
export function hello() {
  return "workflow-engine";
}
