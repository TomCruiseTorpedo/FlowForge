{
  "name": "flowforge",
  "version": "0.1.0",
  "private": true,
  "description": "FlowForge AI Automations — monorepo",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "start": "pnpm --filter api start",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0"
}
