# -----------------------------------------------------------------------------
# DO NOT REMOVE THIS BLOCK — Required for public repo safety.
# If a script or tool rewrites .gitignore, re-add this block immediately.
# Recovery and prevention docs live in docs/recovery/ (docs/ is below).
# Private / do not publish (public repo must not expose these)
# These are also synced to the internal repo for private backup.
# -----------------------------------------------------------------------------
.agents/
.beads/
.claude/
.cline/
.clinerules/
.github/
.groupcode/
docs/
AGENTS.md
RECOVERY.md
RECOVERY-PREVENTION.md
*.code-workspace
scripts/internal/
.vscode/

# -----------------------------------------------------------------------------
# Local runtime & temporary files (not tracked, NOT privately synced)
# Large/regenerable; no need in repo or in internal backup.
# -----------------------------------------------------------------------------
node_modules/
.pnpm-store/

# OS/editor noise
.DS_Store
.idea/

# Environment and secrets
.env
.env.*
!.env.example

# Logs and runtime artifacts
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Node artifacts (future scaffold)
node_modules/
dist/
build/
coverage/
.next/

# Lovable re-integration source (clone; do not commit)
flowforge-focus/
*.db
.pnpm-store/
